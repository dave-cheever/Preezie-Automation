INPUTS:

PromptArguments (contains userPrompt, genders, dynamicVariantFields):
{{PromptArguments}}

LLMRequestFormattedPrompt:
{{LLMRequestFormattedPrompt}}

UserMessage:
{{UserMessage}}

AssistantResponse (Product Search Query Result):
{{ResponseLLM}}

TASK:

IMPORTANT: You are validating ONLY whether the LLM's response is correct given the instructions.
DO NOT judge or evaluate the user's message itself - it can be anything.
Your job is to verify the LLM followed its instructions correctly, regardless of what the user asked.

1. Review the LLMRequestFormattedPrompt to understand:
   - The expected output format for search queries
   - The rules for extracting search terms and filters
2. Review PromptArguments to know the valid options:
   - genders: the available gender filter options
   - dynamicVariantFields: the available product attribute filters
3. Given the UserMessage (accept it as-is, don't judge if it's appropriate):
   - Did the LLM correctly apply the extraction rules from the prompt?
   - Are returned filters using only valid values from genders/dynamicVariantFields?
4. Validate that the AssistantResponse:
   - Follows the output format specified in the prompt
   - Uses only valid filter values from the available options
   - Correctly applies the extraction logic per the prompt

SCORING RULES (0–5):
- relevance: the response correctly addresses the extraction task as defined in the prompt
- faithfulness: no hallucinated filters or values not in the available options
- instructionCompliance: follows the exact rules and format defined in LLMRequestFormattedPrompt
- semanticCloseness: the extraction matches what the prompt instructions dictate

PASS CONDITIONS:
- relevance >= 4
- faithfulness >= 4
- instructionCompliance >= 4
- semanticCloseness >= 4

RETURN ONLY THIS JSON STRUCTURE:

{
  "pass": true | false,
  "scores": {
    "relevance": number,
    "faithfulness": number,
    "instructionCompliance": number,
    "semanticCloseness": number
  },
  "extractedQuery": "summary of search query/filters from AssistantResponse",
  "expectedQuery": "what the prompt rules dictate should be extracted",
  "issues": [string],
  "summary": "short explanation of whether the LLM correctly followed its extraction instructions"
}

