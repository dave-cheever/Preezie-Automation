INPUTS:

PromptArguments (contains userPrompt, brandOverview):
{{PromptArguments}}

LLMRequestFormattedPrompt:
{{LLMRequestFormattedPrompt}}

UserMessage:
{{UserMessage}}

AssistantResponse (User Information Extraction Result):
{{ResponseLLM}}

TASK:

IMPORTANT: You are validating ONLY whether the LLM's response is correct given the instructions.
DO NOT judge or evaluate the user's message itself - it can be anything.
Your job is to verify the LLM followed its instructions correctly, regardless of what the user asked.

CRITICAL: You do NOT have ground truth. You CANNOT determine what information "should" have
been extracted — you have the same inputs as the LLM. Only flag a FAILURE if it is PROVABLY
WRONG from the provided data:
- The response fabricates information not present in the input
- The output format is incorrect
- Instructions are clearly violated
DO NOT fail the response because you would have extracted different valid information.

1. Review the LLMRequestFormattedPrompt to understand:
   - The expected output format for user information extraction
   - The rules for extracting user details
2. Review PromptArguments for context about what data is available.
3. Given the UserMessage (accept it as-is, don't judge if it's appropriate):
   - Is the extracted information grounded in the actual input?
   - Is the format correct?
4. Validate that the AssistantResponse:
   - Follows the output format specified in the prompt
   - Extracts ONLY information actually present in the input (no fabrications)
   - Follows the extraction guidelines per the prompt

ISSUE CITATION REQUIREMENT:
For EVERY issue you report, you MUST include:
- The rule reference from the LLMRequestFormattedPrompt (e.g. "Rule 1", "Section Name", or rule title)
- A short verbatim quote of the violated rule text
Format each issue as: "[Rule X / Section Name] '<quoted rule text>' — <explanation of violation>"

SCORING RULES (0–5):
- relevance: the response addresses the extraction task as defined in the prompt
- faithfulness: no fabricated user info — all extracted info is grounded in actual input
- instructionCompliance: follows the exact format and structural rules defined in LLMRequestFormattedPrompt
- semanticCloseness: the response structure and format match what the prompt describes

PASS CONDITIONS:
- relevance >= 4
- faithfulness >= 4
- instructionCompliance >= 4
- semanticCloseness >= 4

RETURN ONLY THIS JSON STRUCTURE:

{
  "pass": true | false,
  "scores": {
    "relevance": number,
    "faithfulness": number,
    "instructionCompliance": number,
    "semanticCloseness": number
  },
  "extractedInfo": "summary of user information extracted from AssistantResponse",
  "promptRulesSummary": "1-2 sentence summary of the extraction rules in the prompt (what fields to extract, output format, what to do when info is absent)",
  "issues": ["[Rule X / Section] '<quoted rule text>' — explanation of violation"],
  "summary": "short explanation of whether the LLM correctly followed its extraction instructions"
}


