INPUTS:

PromptArguments (contains userPrompt, brandOverview):
{{PromptArguments}}

LLMRequestFormattedPrompt:
{{LLMRequestFormattedPrompt}}

UserMessage:
{{UserMessage}}

AssistantResponse (User Information Extraction Result):
{{ResponseLLM}}

TASK:

IMPORTANT: You are validating ONLY whether the LLM's response is correct given the instructions.
DO NOT judge or evaluate the user's message itself - it can be anything.
Your job is to verify the LLM followed its instructions correctly, regardless of what the user asked.

1. Review the LLMRequestFormattedPrompt to understand:
   - The expected output format for user information extraction
   - The rules for extracting user details
2. Review PromptArguments for context about what data is available.
3. Given the UserMessage (accept it as-is, don't judge if it's appropriate):
   - Did the LLM correctly apply the extraction rules from the prompt?
   - Is the extracted information grounded in the actual input?
4. Validate that the AssistantResponse:
   - Follows the output format specified in the prompt
   - Extracts ONLY information actually present in the input (no fabrications)
   - Follows the extraction guidelines per the prompt

SCORING RULES (0–5):
- relevance: the response follows the prompt's extraction guidelines
- faithfulness: no fabricated user info - all extracted info is grounded in actual input
- instructionCompliance: follows the exact rules and format defined in LLMRequestFormattedPrompt
- semanticCloseness: the extraction matches what the prompt instructions dictate

PASS CONDITIONS:
- relevance >= 4
- faithfulness >= 4
- instructionCompliance >= 4
- semanticCloseness >= 4

RETURN ONLY THIS JSON STRUCTURE:

{
  "pass": true | false,
  "scores": {
    "relevance": number,
    "faithfulness": number,
    "instructionCompliance": number,
    "semanticCloseness": number
  },
  "extractedInfo": "summary of user information extracted from AssistantResponse",
  "expectedInfo": "what information should be extracted based on the prompt rules",
  "issues": [string],
  "summary": "short explanation of whether the LLM correctly followed its extraction instructions"
}


