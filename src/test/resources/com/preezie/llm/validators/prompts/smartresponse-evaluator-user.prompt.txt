INPUTS:

PromptArguments (contains userPrompt, baseProduct, products):
{{PromptArguments}}

LLMRequestFormattedPrompt:
{{LLMRequestFormattedPrompt}}

UserMessage:
{{UserMessage}}

AssistantResponse (Smart Response Result):
{{ResponseLLM}}

TASK:

IMPORTANT: You are validating ONLY whether the LLM's response is correct given the instructions.
DO NOT judge or evaluate the user's message itself - it can be anything.
Your job is to verify the LLM followed its instructions correctly, regardless of what the user asked.

1. Review the LLMRequestFormattedPrompt to understand:
   - The expected output format and conversational rules
   - Guidelines for product recommendations and responses
2. Review PromptArguments for the GROUND TRUTH data:
   - baseProduct: the main product being discussed (if any)
   - products: the available products data for recommendations
3. Given the UserMessage (accept it as-is, don't judge if it's appropriate):
   - Did the LLM correctly follow the response rules from the prompt?
   - Is the response grounded in the provided product data?
4. Validate that the AssistantResponse:
   - Follows the output format specified in the prompt
   - Uses ONLY information from the provided product data (no hallucinations)
   - Follows the conversational guidelines per the prompt
   - Does not fabricate product features, prices, or availability

Use warnings for minor omissions or partial instruction misses when the response is still partially acceptable.
Use failure only when the response is empty, completely irrelevant, severely out of context, obviously hallucinated, or violates all or most of the instructions.
If the response is grounded, on-topic, and mostly follows the product validation format, but has formatting slips such as imperfect product headings, slightly incorrect product ordering, or an overlong closing sentence, mark it as warning instead of failure.
Do not fail solely because the response mixes product validation style with a mild narrowing-style closing sentence if the rest of the answer is still usable.

ISSUE CITATION REQUIREMENT:
For EVERY issue you report, you MUST include:
- The rule reference from the LLMRequestFormattedPrompt (e.g. "Rule 1", "Section Name", or rule title)
- A short verbatim quote of the violated rule text
Format each issue as: "[Rule X / Section Name] '<quoted rule text>' — <explanation of violation>"

SCORING RULES (0–5):
- relevance: the response follows the prompt's response guidelines
- faithfulness: no hallucinated product details - all info grounded in provided data
- instructionCompliance: follows the exact rules and format defined in LLMRequestFormattedPrompt
- semanticCloseness: the response type matches what the prompt instructions dictate

PASS CONDITIONS:
- relevance >= 4
- faithfulness >= 4
- instructionCompliance >= 4
- semanticCloseness >= 4

RETURN ONLY THIS JSON STRUCTURE:

{
  "pass": true | false,
  "severity": "pass" | "warning" | "fail",
  "scores": {
    "relevance": number,
    "faithfulness": number,
    "instructionCompliance": number,
    "semanticCloseness": number
  },
  "responseAnalysis": "brief description of what the LLM response actually contains",
  "warnings": ["optional list of mild issues that should not fail the test"],
  "promptRulesSummary": "1-2 sentence summary of the key response rules in the prompt (format, tone, grounding requirements, what product data must be used)",
  "issues": ["[Rule X / Section] '<quoted rule text>' — explanation of violation"],
  "summary": "short explanation of whether the LLM correctly followed its response instructions"
}
