INPUTS:

PromptArguments (contains userPrompt, brandOverview, lastDiscussedCategories):
{{PromptArguments}}

LLMRequestFormattedPrompt:
{{LLMRequestFormattedPrompt}}

UserMessage:
{{UserMessage}}

AssistantResponse (Category Classification Result):
{{ResponseLLM}}

TASK:

IMPORTANT: You are validating ONLY whether the LLM's response is correct given the instructions.
DO NOT judge or evaluate the user's message itself - it can be anything.
Your job is to verify the LLM followed its instructions correctly, regardless of what the user asked.

CRITICAL: You do NOT have ground truth. You CANNOT determine which categories "should" have
been selected — you have the same inputs as the LLM. Only flag a FAILURE if it is PROVABLY
WRONG from the provided data:
- A returned category is NOT in the taxonomy listed in the prompt
- The output format is incorrect
- Instructions are clearly violated
DO NOT fail the response because you would have selected different valid categories.

1. Review the LLMRequestFormattedPrompt to understand:
   - What categories/taxonomy are available
   - What the classification rules and criteria are
   - The expected output format
2. Given the UserMessage (accept it as-is, don't judge if it's appropriate):
   - Are the returned categories valid entries from the available taxonomy?
   - Is the format correct as specified?
3. Validate that the AssistantResponse:
   - Follows the output format specified in the prompt
   - Returns only valid categories from the available taxonomy (this is the ONLY correctness check)

ISSUE CITATION REQUIREMENT:
For EVERY issue you report, you MUST include:
- The rule reference from the LLMRequestFormattedPrompt (e.g. "Rule 1", "Section Name", or rule title)
- A short verbatim quote of the violated rule text
Format each issue as: "[Rule X / Section Name] '<quoted rule text>' — <explanation of violation>"

SCORING RULES (0–5):
- relevance: the response addresses the classification task and uses values from the taxonomy
- faithfulness: no hallucinated categories — every returned category exists in the available taxonomy
- instructionCompliance: follows the exact format and structural rules defined in LLMRequestFormattedPrompt
- semanticCloseness: the response structure and format match what the prompt describes

PASS CONDITIONS:
- relevance >= 4
- faithfulness >= 4
- instructionCompliance >= 4
- semanticCloseness >= 4

RETURN ONLY THIS JSON STRUCTURE:

{
  "pass": true | false,
  "scores": {
    "relevance": number,
    "faithfulness": number,
    "instructionCompliance": number,
    "semanticCloseness": number
  },
  "classifiedCategories": "list or summary of categories from AssistantResponse",
  "validOptionsFromPrompt": "list of valid categories extracted from the taxonomy in LLMRequestFormattedPrompt",
  "promptRulesSummary": "1-2 sentence summary of the classification rules in the prompt (format, selection logic, how many categories to return)",
  "issues": ["[Rule X / Section] '<quoted rule text>' — explanation of violation"],
  "summary": "short explanation of whether the LLM correctly followed its classification instructions"
}

