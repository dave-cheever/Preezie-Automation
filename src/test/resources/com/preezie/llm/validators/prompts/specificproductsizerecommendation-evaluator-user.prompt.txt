INPUTS:

PromptArguments (contains user profile, tone of voice, brand overview, user input, product size data, and product name):
{{PromptArguments}}

LLMRequestFormattedPrompt (the instructions given to the LLM):
{{LLMRequestFormattedPrompt}}

UserMessage (the user's question about product size recommendation):
{{UserMessage}}

AssistantResponse (the LLM's size recommendation response):
{{ResponseLLM}}

TASK:

IMPORTANT: You are validating ONLY whether the LLM's response is correct given the instructions.
DO NOT judge or evaluate the user's message itself - it can be anything (size questions, random text, etc.).
Your job is to verify the LLM followed its instructions correctly, regardless of what the user asked.

CRITICAL: You do NOT have ground truth. You CANNOT determine what the "correct" size
recommendation "should" have been — you have the same inputs as the LLM. Only flag a
FAILURE if it is PROVABLY WRONG from the provided data:
- The response fabricates size data not present in the provided product size data
- The output format is incorrect
- Instructions are clearly violated
DO NOT fail the response because you would have recommended differently.

1. Review the LLMRequestFormattedPrompt to understand:
   - What response format is expected
   - What rules govern how to provide size recommendations
   - What user profile and product size data should be used
   - How edge cases should be handled
2. Given the UserMessage (accept it as-is, don't judge if it's appropriate):
   - Is the response format correct as specified?
   - Does the response use only the provided product size data and user profile (no fabrication)?
   - Does the response match the specified tone of voice and brand overview?
3. Evaluate if the response is:
   - Faithful to the instructions in the prompt
   - Properly formatted according to requirements
   - Free of fabricated size data not in the provided product size information

ISSUE CITATION REQUIREMENT:
For EVERY issue you report, you MUST include:
- The rule reference from the LLMRequestFormattedPrompt (e.g. "Rule 1", "Section Name", or rule title)
- A short verbatim quote of the violated rule text
Format each issue as: "[Rule X / Section Name] '<quoted rule text>' — <explanation of violation>"

Score each aspect 0-10:
- relevance: How well does the response follow the prompt's format and guidelines?
- faithfulness: Does the response use only provided size data? No fabricated measurements?
- instructionCompliance: Did the LLM follow ALL specified format and structural instructions?
- semanticCloseness: How well does the response structure match what the prompt describes?

Pass threshold: All scores >= 7 AND instructionCompliance >= 8

Return your evaluation as JSON:
{
  "pass": true | false,
  "scores": {
    "relevance": number,
    "faithfulness": number,
    "instructionCompliance": number,
    "semanticCloseness": number
  },
  "responseAnalysis": "brief description of what the LLM size recommendation response actually contains",
  "promptRulesSummary": "1-2 sentence summary of the key rules in the prompt (format, tone, how to use size data and user profile)",
  "issues": ["[Rule X / Section] '<quoted rule text>' — explanation of violation"],
  "summary": "short explanation of whether the LLM correctly followed its size recommendation instructions"
}



