INPUTS:

PromptArguments (contains userPrompt, genders, dynamicVariantFields):
{{PromptArguments}}

LLMRequestFormattedPrompt:
{{LLMRequestFormattedPrompt}}

UserMessage:
{{UserMessage}}

AssistantResponse (Product Search Query Result):
{{ResponseLLM}}

TASK:

IMPORTANT: You are validating ONLY whether the LLM's response correctly follows the LLMRequestFormattedPrompt.
DO NOT judge or evaluate the user's message itself - it can be anything.

CRITICAL — UNDERSTAND WHAT EACH INPUT CONTROLS:
- `genders`: controls ONLY the allowed values for the `Gender` field. Nothing else.
- `dynamicVariantFields`: these are store-specific structured variant dimension labels (e.g. Size options).
  They do NOT control what goes in `AdditionalKeywords`.
  `AdditionalKeywords` is a FREE-TEXT search keyword enhancement field. The LLM may include any explicit
  attribute mentioned in the user query (fabric, material, sub-type, style, sub-type) that is not already
  captured by other fields (RawColor, PrimaryColor, DisplaySize, price, sale, newarrival, gender, brand).
  The rules for AdditionalKeywords are defined in the ADDITIONALKEYWORDS SPEC section of the LLMRequestFormattedPrompt.

CRITICAL — ONLY FLAG FAILURES THAT ARE PROVABLY WRONG:
You CANNOT determine what search terms "should" have been extracted.
Only flag a FAILURE if a specific, named rule in the LLMRequestFormattedPrompt is clearly violated.
DO NOT fail the response because you would have extracted different valid terms.
Use "warning" for minor omissions or partial instruction misses when the response is still partially acceptable.
Use "fail" only when the response is empty, completely irrelevant, hallucinated, or breaks the prompt so badly that it is unusable.

STEPS:
1. Read the LLMRequestFormattedPrompt fully — understand the GENERAL RULES, ADDITIONALKEYWORDS SPEC, and JSON schema.
2. Check the `Gender` field: is its value in the `genders` list from PromptArguments? If not, flag it.
3. Check `AdditionalKeywords` against the ADDITIONALKEYWORDS SPEC rules (not dynamicVariantFields):
   - Does it contain generic category/type terms? (Rule 2 + ADDITIONALKEYWORDS SPEC forbid this)
   - Does it contain values already represented by RawColor, PrimaryColor, price, sale, newarrival, gender, brand? (forbidden by ADDITIONALKEYWORDS SPEC)
   - Are there more than 3 variant strings? (ADDITIONALKEYWORDS SPEC limit)
   - Is "preeziepriority" included as a separate variant string? (Rule 13 requires this)
4. Check all output values are lowercased (Rule 8 requires this).
5. Check the JSON field order and structure matches the schema exactly.
6. Do NOT flag linen, fabric, material, or other explicit query attributes as violations — these are valid AdditionalKeywords.

ISSUE CITATION REQUIREMENT:
For EVERY issue you report, you MUST include:
- The rule reference (e.g. "Rule 2", "Rule 8", "ADDITIONALKEYWORDS SPEC", "Rule 13")
- A short verbatim quote of the violated rule text from LLMRequestFormattedPrompt
Format each issue as: "[Rule X / Section Name] '<quoted rule text>' — <explanation of violation>"

SCORING RULES (0–5):
- relevance: the response addresses the extraction task for the user query
- faithfulness: output only contains values/fields grounded in the prompt rules and user query
- instructionCompliance: accurately follows every rule in the LLMRequestFormattedPrompt
- semanticCloseness: the response structure and format match the specified JSON schema

PASS CONDITIONS:
- relevance >= 4
- faithfulness >= 4
- instructionCompliance >= 4
- semanticCloseness >= 4

SEVERITY RULES:
- "pass": the response is correct and grounded.
- "warning": the response is mostly on-task but has mild issues that do not make it unusable.
- "fail": the response is empty, irrelevant, hallucinated, or clearly breaks the required instructions.

RETURN ONLY THIS JSON STRUCTURE:

{
  "pass": true | false,
  "severity": "pass" | "warning" | "fail",
  "scores": {
    "relevance": number,
    "faithfulness": number,
    "instructionCompliance": number,
    "semanticCloseness": number
  },
  "extractedQuery": "summary of what the LLM extracted: color, price, size, keywords, etc.",
  "warnings": ["optional list of mild issues that should not fail the test"],
  "promptRulesSummary": "1-2 sentence factual summary of the key extraction rules from LLMRequestFormattedPrompt (what fields to extract, what AdditionalKeywords rules apply, lowercase requirement, preeziepriority rule)",
  "issues": ["[Rule X / Section] '<quoted rule>' — explanation of how the response violated it"],
  "summary": "short explanation of whether the LLM correctly followed its extraction instructions"
}
